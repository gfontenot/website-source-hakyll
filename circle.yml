machine:
  ghc:
    version: 7.10.1

dependencies:
  override:
    - ./bin/setup

test:
  override:
    - cabal run --  clean
    - cabal run --  build
    - cabal run --  check --internal-links

deployment:
  master:
    branch: master
    commands:
      - ./bin/ci-deploy
