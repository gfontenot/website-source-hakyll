machine:
  ghc:
    version: 7.10.1

test:
  override:
    - cabal install --disable-documentation
    - site clean
    - site build
    - site check --internal-links

deployment:
  master:
    branch: master
    commands:
      - ./bin/ci-deploy
