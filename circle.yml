machine:
  ghc:
    version: 7.10.1

test:
  override:
    - cabal build
    - ./site clean
    - ./site build
    - ./site check --internal-links

deployment:
  master:
    branch: master
    commands:
      - ./bin/ci-deploy
